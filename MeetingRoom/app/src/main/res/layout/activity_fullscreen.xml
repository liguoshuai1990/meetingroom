<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_fullscreen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background2"
    tools:context="com.zrlgs.meetingRoom.FullscreenActivity"
    android:orientation="vertical">

    <LinearLayout
        style="?metaButtonBarStyle"
        android:id="@+id/layout_head"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/meeting_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="30sp"
            android:layout_marginTop="10dp"
            android:text="会议室 2-01"
            android:textStyle="normal|bold" />

        <include layout="@layout/fragment_timer" />

        <include layout="@layout/fragment_title_button" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/layout_main_frame"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3">
    </FrameLayout>

</LinearLayout>
